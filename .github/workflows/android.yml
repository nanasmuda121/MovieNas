name: Build Native Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Grant Execute Permission for Gradlew
        working-directory: ./android
        run: chmod +x gradlew

      - name: Build Debug APK
        working-directory: ./android
        run: gradle assembleDebug --stacktrace

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MovieNas-Debug-APK
          path: android/app/build/outputs/apk/debug/*.apk

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main'
        with:
          tag_name: v1.0.0-build-${{ github.run_number }}
          name: MovieNas Native Android v1.0.0 (Build ${{ github.run_number }})
          body: |
            ### 🎬 MovieNas Native Android APK
            Aplikasi Android streaming film, serial TV, dan drama pendek.
            
            - **Bypass CDN:** Menggunakan header referer resmi, streaming langsung dari CDN tanpa server proxy!
            - **Player:** AndroidX Media3 / ExoPlayer dengan multi-resolusi (360p, 480p, 720p, 1080p).
            - **Commit:** ${{ github.sha }}
            
            Unduh file `.apk` di bawah dan install langsung di HP Android Anda!
          files: |
            android/app/build/outputs/apk/debug/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
